/openclaw
/data
.env
client_secret_*.json
/.bak/
# .openclaw is a separate git repo (synced via git, not tracked by parent)
/.openclaw/

# Workspaces git repo (synced via GitHub)
/workspaces/

# gws authentication files (now in .openclaw/.config/gws/)
/.config/
/Library/
