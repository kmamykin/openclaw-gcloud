/openclaw
/data
.env
client_secret_*.json

# .openclaw is a separate git repo (synced via git, not tracked by parent)
/.openclaw/

# Workspaces are separate git repos (synced via GitHub)
/workspace-*/

# gogcli authentication files (now in .openclaw/.config/gogcli/)
/.config/
/Library/
/.gog-temp/
